sudo apt update
sudo apt install python3-pip

sudo apt install awscli
aws configure
aws s3 cp s3://midd-brats/brats_data brats_data --recursive

!! Note that the aws thing doesnt work on gcp !!
!!! need to install using the method below (installs aws2 instead of aws) !!!
curl "https://d1vvhvl2y92vvt.cloudfront.net/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

git clone https://github.com/toehmler/glioma-segmentation
pip3 install tensorflow SimpleITK numpy scikit-learn tqdm keras
pip3 install SimpleITK
pip3 install numpy
pip3 install scikit-learn 
pip3 install tqdm
pip3 install keras tensorflow
pip3 install matplotlib imageio scikit-image

* allow udp firewall 60000-61000 *
sudo apt install mosh

todo:
- tensorboard

